# Free-tier backend blueprint for Render.com
# Docs: https://render.com/docs/blueprint-spec
#
# Background workers are NOT available on Render Free.
#
# After deploy, Shell on rfidcore-api:
#   npx prisma migrate deploy -w apps/api
#   npx prisma db seed -w apps/api

services:
  - type: web
    name: rfidcore-api
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm ci && npx prisma generate -w apps/api && npm run build -w apps/api
    startCommand: npm run start -w apps/api
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_PORT
        value: "4000"
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        sync: false
      - key: S3_ENDPOINT
        value: ""
      - key: SMTP_FROM
        value: RFIDCore <notifications@rfidcore.local>
