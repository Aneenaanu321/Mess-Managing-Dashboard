# Free-tier backend blueprint for Render.com
# Docs: https://render.com/docs/blueprint-spec
#
# Background workers are NOT available on Render Free.
#
# After deploy, Shell on rfidcore-api:
#   npm run prisma:deploy -w apps/api
#   npm run prisma:seed -w apps/api

services:
  - type: web
    name: rfidcore-api
    runtime: node
    plan: free
    rootDir: .
    # Use workspace scripts — `npx prisma generate -w` does NOT work and breaks tsc.
    buildCommand: npm ci && npm run prisma:generate -w apps/api && npm run build -w apps/api
    startCommand: npm run start -w apps/api
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_PORT
        value: "4000"
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        sync: false
      - key: S3_ENDPOINT
        value: ""
      - key: SMTP_FROM
        value: RFIDCore <notifications@rfidcore.local>
